name: music_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Networking + Subsonic (Navidrome) integration.
  http: ^1.2.0
  crypto: ^3.0.3 # md5 salted-token auth for Subsonic
  shared_preferences: ^2.2.0 # persist the server connection

  # Audio playback.
  just_audio: ^0.9.42
  # Foreground media service + notification so playback (and its network
  # connection) survives screen-off / Doze instead of stalling.
  just_audio_background: ^0.0.1-beta.11
  # Separate lightweight player for 30s catalogue previews — just_audio_background
  # only permits one just_audio instance, so previews use their own pipeline.
  audioplayers: ^6.0.0
  solar_icons: ^0.1.0
  # Pick a gallery image to use as a custom playlist cover or account picture.
  image_picker: ^1.1.2
  # MediaType, to label uploaded images with their real content type (http's
  # MultipartFile otherwise declares everything application/octet-stream).
  http_parser: ^4.1.2
  # Record a few seconds of audio for the "what's playing?" identifier, and a
  # temp directory to hold the clip while it's uploaded.
  record: ^7.1.1
  path_provider: ^2.1.6
  file_picker: ^12.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# Vendored just_audio, patched to force AUDIO_OFFLOAD_MODE_DISABLED. Upstream
# 0.9.46 hard-enables ExoPlayer audio offload, which on some devices (e.g.
# Pixel) routes the stream to the DSP's compress-offload path and plays silent
# (volume 0) while the clock still advances. Decoding to PCM restores audio.
dependency_overrides:
  just_audio:
    path: packages/just_audio
  # Forked to customise the media notification: the stop button is dropped and
  # like/shuffle actions added, neither of which the published package exposes.
  just_audio_background:
    path: packages/just_audio_background

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The families offered under Settings > Appearance > Font. Adding one here
  # and to AppFont in services/appearance_state.dart is all a new choice needs.
  fonts:
    - family: Nunito
      fonts:
        # Variable font — Flutter maps TextStyle.fontWeight onto its wght axis.
        - asset: assets/fonts/Nunito-VariableFont.ttf
    - family: Open Sans
      fonts:
        - asset: assets/fonts/OpenSans-Regular.ttf
        - asset: assets/fonts/OpenSans-Semibold.ttf
          weight: 600
        - asset: assets/fonts/OpenSans-Bold.ttf
          weight: 700
        - asset: assets/fonts/OpenSans-ExtraBold.ttf
          weight: 800
    - family: Roboto Slab
      fonts:
        - asset: assets/fonts/RobotoSlab-Regular.ttf
        - asset: assets/fonts/RobotoSlab-Bold.ttf
          weight: 700
    - family: Inter
      fonts:
        # Variable font, like Nunito — one file covers every weight.
        - asset: assets/fonts/InterVariable.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
